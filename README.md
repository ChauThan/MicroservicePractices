## Stop Pods
```ps
kubectl scale deploy commands-depl --replicas=0
kubectl scale deploy commands-depl --replicas=0
```

## Start Pods
```ps
kubectl scale deploy commands-depl --replicas=1
kubectl scale deploy commands-depl --replicas=1
```